# transactions

Shows explicit `begin()/commit()`, savepoints, explicit rollback, and closure-style transactions.

Run from the repository root:

```bash
make db-up
cargo run --manifest-path samples/transactions/Cargo.toml
```
