# JSON Search

`SearchRequest` accepts client filters, sort, limit, and offset. Rust still owns
the table, projection, joins, raw SQL, and server filters.

`src/schema.rs` is generated by `rqb-cli` from `../schema.sql`.
