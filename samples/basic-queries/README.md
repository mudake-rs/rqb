# basic-queries

Shows SELECT, INSERT, UPDATE, DELETE, complex Rust-side filters, and mapping a
partial projection into a small result struct.

Run from the repository root:

```bash
make db-up
cargo run --manifest-path samples/basic-queries/Cargo.toml
```
