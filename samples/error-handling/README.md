# error-handling

Shows common app error patterns: constraint mapping, `NotFound` to `Option`, and validation errors.

Run from the repository root:

```bash
make db-up
cargo run --manifest-path samples/error-handling/Cargo.toml
```
